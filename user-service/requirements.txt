# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # Compatible version with passlib 1.7.4

# Email validation for Pydantic
email-validator==2.1.0

# Shared package (installed separately)
# platform-shared

# Configuration
python-dotenv==1.0.0
pydantic-settings==2.2.0
pydantic>=2.5.0

# HTTP client (used in health checks, tests, etc.)
requests==2.31.0

# Kafka (for future event publishing)
kafka-python==2.0.2

