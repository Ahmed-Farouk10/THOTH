# --- Core API & DB ---
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
pydantic>=2.7.0
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0

# --- Infrastructure ---
boto3==1.34.0
kafka-python==2.0.2
python-dotenv==1.0.0
httpx==0.25.2  # For Google Gemini API calls

# --- Document Processing ---
PyPDF2==3.0.1
pdfminer.six==20221105
pdf2image==1.16.3
pytesseract==0.3.10
PyMuPDF==1.23.8
python-docx==1.1.0
spacy==3.7.2
# Pre-downloaded spacy model handled in Dockerfile or startup script

# --- OCR Libraries ---
# EasyOCR is optional (large ~500MB). Install manually if ENABLE_EASYOCR=true.
# easyocr==1.7.0  # Optional fallback OCR (install only when needed)
opencv-python-headless==4.8.1.78  # Headless OpenCV for preprocessing
Pillow==10.1.0  # Image processing

